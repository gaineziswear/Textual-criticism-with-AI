[build]
  base = "web"
  publish = "dist"
  command = "npm run build"

# SPA fallback for the React/Vite application.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
[functions]
  directory = "netlify/functions"

# Keep production builds on the Git-integrated Netlify pipeline.
